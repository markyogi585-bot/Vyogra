import { BadgeIndianRupee, CheckCircle2, FileClock, Mail, RefreshCw, Send, Stamp } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { AdminPageFrame } from "@/components/admin/AdminPageFrame";
import { InvoicePreview } from "@/components/commerce/InvoicePreview";
export default function AdminInvoicePage() { const [status, setStatus] = useState("Issued"); return <AdminPageFrame eyebrow="FINANCE / INVOICE DESK" title={<>The record behind<br /><i>the journey.</i></>}><p className="admin-page-lead">Review the traveler’s billing snapshot, mark payment state, resend documents, and keep every adjustment visible to finance and support.</p><div className="admin-invoice-layout"><InvoicePreview invoiceNumber="INV-VYG-26-43071" bookingCode="VYG-2026-43071" total={36_873} status={status} /><aside className="admin-invoice-controls"><section><span><BadgeIndianRupee size={16} /> PAYMENT STATE</span><h2>₹36,873 due.</h2><div className="admin-invoice-statuses">{["Issued", "Paid", "Refunded"].map((item) => <button className={status === item ? "active" : ""} onClick={() => { setStatus(item); toast(`Invoice marked ${item.toLowerCase()}.`); }} key={item}>{item === "Paid" && <CheckCircle2 size={14} />}{item === "Issued" && <FileClock size={14} />}{item === "Refunded" && <RefreshCw size={14} />}{item}</button>)}</div></section><section><span><Stamp size={16} /> BILLING SNAPSHOT</span><p><b>Aarav Mehta</b><small>aarav@example.com</small><small>+91 99714 06264</small></p><p><b>GST treatment</b><small>5% travel service tax</small><small>Campaign credit: GOA2500</small></p></section><section><span><Mail size={16} /> DELIVERY</span><button onClick={() => toast("Invoice delivery queued to verified traveler contact.")}><Send size={15} /> Resend secure invoice</button><button onClick={() => toast("The secure invoice link is ready to share.")}><Mail size={15} /> Share access link</button></section><section className="admin-invoice-audit"><span>IMMUTABLE HISTORY</span><p><b>Invoice issued</b><small>Today · 10:26 · Admin</small></p><p><b>Terms receipt attached</b><small>Today · 10:26 · System</small></p><p><b>Access grant created</b><small>Today · 10:26 · System</small></p></section></aside></div></AdminPageFrame>; }
