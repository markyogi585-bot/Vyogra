import { CalendarDays, ChevronRight, Download, FileText, MapPinned, MessageCircle, Share2, ShieldCheck, TicketCheck, UserRound } from "lucide-react";
import { Link } from "wouter";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";

export default function BookingPortalPage({ bookingCode }: { bookingCode: string }) {
  const access = trpc.bookingAccess.current.useQuery({ bookingCode }, { enabled: Boolean(bookingCode), retry: false });
  if (access.isLoading) return <main className="booking-portal-page"><section className="booking-portal-hero"><span>VOYAGR TRIP DESK</span><h1>Opening your<br /><i>secure journey.</i></h1><p>Verifying the short-lived secure desk session and preparing your travel record.</p></section></main>;
  if (!access.data?.verified) return <main className="booking-portal-page"><section className="booking-portal-hero"><span>BOOKING-ID ACCESS</span><h1>Verify before<br /><i>you continue.</i></h1><p>This trip desk requires a valid short-lived access session. Verify your booking ID and traveler contact suffix again to continue.</p><Link href="/access">Verify booking access <ChevronRight size={16} /></Link></section></main>;
  const { booking, travelers, invoice, expiresAt } = access.data;
  return <main className="booking-portal-page"><header className="booking-portal-header"><Link href="/app">VOYAGR</Link><div><span>{booking.bookingCode}</span><Link href="/support">Support</Link></div></header><section className="booking-portal-hero"><span>YOUR {booking.status.toUpperCase()} JOURNEY</span><h1>{booking.packageName}</h1><p>{booking.destination} · {booking.durationNights} nights · {booking.durationDays} days · {booking.travelerCount} travelers</p><div><span><BadgeIcon /><b>{booking.status.toUpperCase()}</b></span><button onClick={() => toast("A secure trip-desk link can be issued through traveler support.")}><Share2 size={16} /> Share</button></div></section><section className="booking-portal-grid"><div className="booking-portal-main"><section className="booking-panel itinerary"><div className="booking-panel-heading"><div><span>LIVE TRIP DESK</span><h2>Before you leave.</h2></div><Link href={`/trips/live?booking=${booking.bookingCode}`}><MapPinned size={18} /> Track trip</Link></div><div className="booking-timeline-row"><i /><p><b>Booking {booking.status}</b>Your confirmed departure, itinerary, and host updates appear here as the trip moves forward.</p></div><div className="booking-timeline-row"><i /><p><b>Trip updates</b>Live milestones and uploaded photos are released by the host team to this verified desk.</p></div></section><section className="booking-panel"><div className="booking-panel-heading"><div><span>TRAVELERS</span><h2>Your party</h2></div><UserRound size={18} /></div>{travelers.length ? travelers.map((traveler, index) => <div className="traveler-row" key={`${traveler.fullName}-${index}`}><span>{traveler.fullName.slice(0, 2).toUpperCase()}</span><div><b>{traveler.fullName}</b><small>{traveler.email || traveler.phone || "Traveler details verified"}</small></div><strong>{index === 0 ? "Lead" : "Traveler"}</strong></div>) : <p className="booking-empty-note">Traveler details will appear when the booking team completes the party record.</p>}</section></div><aside className="booking-portal-side"><section className="booking-panel booking-document-panel"><span>TRIP VAULT</span><h2>Tickets & invoices</h2><Link href={`/access/${booking.bookingCode}`}><TicketCheck size={18} /><span>VOYAGR e-ticket<small>Available when travel documents are issued</small></span><Download size={16} /></Link>{invoice ? <Link href={`/invoice/${booking.bookingCode}`}><FileText size={18} /><span>Tax invoice<small>{invoice.invoiceNumber} · ₹{Number(invoice.grandTotal).toLocaleString("en-IN")}</small></span><ChevronRight size={16} /></Link> : <Link href="/support"><FileText size={18} /><span>Invoice status<small>Request an invoice through traveler support</small></span><ChevronRight size={16} /></Link>}<Link href="/support"><CalendarDays size={18} /><span>Stay confirmation<small>Ask the trip team for your stay record</small></span><ChevronRight size={16} /></Link></section><section className="booking-panel booking-host-panel"><span>TRAVEL SUPPORT</span><h2>Need a host?</h2><p>For route questions, small changes, and the moments between the itinerary.</p><Link href="/support"><MessageCircle size={16} /> Message support</Link></section><section className="booking-safe-note"><ShieldCheck size={17} /><span>Booking access is private and expires {new Date(expiresAt).toLocaleString()}. Reverify through Booking ID access when needed.</span></section></aside></section></main>;
}
function BadgeIcon() { return <span className="booking-confirmed-icon"><ShieldCheck size={14} /></span>; }
