import { FileCheck2 } from "lucide-react";
export function TermsAcceptance({ accepted, onChange, packageName = "this journey" }: { accepted: boolean; onChange: (value: boolean) => void; packageName?: string }) { return <label className="commerce-terms"><input type="checkbox" checked={accepted} onChange={(event) => onChange(event.target.checked)} /><span><FileCheck2 size={18} /></span><p>I have read and accept the package-specific terms, cancellation window, rooming rules, and traveler responsibilities for <b>{packageName}</b>.<small>Acceptance is time-stamped and included in your booking record.</small></p></label>; }
