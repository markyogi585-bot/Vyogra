import { Download, Share2 } from "lucide-react";
import { toast } from "sonner";
import { formatInr } from "@/lib/commerce";
export function InvoicePreview({ invoiceNumber, bookingCode, total, status = "Paid" }: { invoiceNumber: string; bookingCode: string; total: number; status?: string }) { const tax = Math.round(total * 0.05); return <section className="invoice-preview"><header><div><span>HAR HAR MAHADEV TOURS & TRAVELS</span><h1>Tax invoice</h1></div><strong className={status.toLowerCase()}>{status}</strong></header><div className="invoice-meta"><span>Invoice <b>{invoiceNumber}</b></span><span>Booking <b>{bookingCode}</b></span><span>Issued <b>14 Aug 2026</b></span></div><div className="invoice-line"><span>Package travel services</span><b>{formatInr(total - tax)}</b></div><div className="invoice-line"><span>GST & service tax</span><b>{formatInr(tax)}</b></div><div className="invoice-total"><span>Total paid</span><b>{formatInr(total)}</b></div><footer><button onClick={() => toast("Invoice PDF prepared for download.")}><Download size={16} /> Download PDF</button><button onClick={() => toast("Secure invoice link copied.")}><Share2 size={16} /> Share</button></footer></section>; }
