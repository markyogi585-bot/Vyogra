import { Check, Clock3, MapPinned, Navigation } from "lucide-react";
const schedule = [{ time: "08:30", title: "Breakfast at the stay", detail: "Today’s host note is in your trip chat.", state: "done" }, { time: "11:00", title: "Palolem market walk", detail: "Meet Ananya at the main gate.", state: "current" }, { time: "17:45", title: "Sunset paddle", detail: "Bring water and your small day bag.", state: "next" }];
export function TripTimeline() { return <section className="trip-timeline"><div className="timeline-head"><div><span className="admin-overline">TODAY / DAY 03</span><h3>The road, right now.</h3></div><button><Navigation size={15} /> Open directions</button></div>{schedule.map((item) => <div className={`timeline-item ${item.state}`} key={item.title}><span className="timeline-marker">{item.state === "done" ? <Check size={13} /> : item.state === "current" ? <MapPinned size={13} /> : <Clock3 size={13} />}</span><div><b>{item.time} · {item.title}</b><p>{item.detail}</p></div></div>)}</section>; }
