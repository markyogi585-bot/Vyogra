import { ArrowRight, BadgeCheck, LockKeyhole } from "lucide-react";
export function BookingAccessCard({ code, contact, setCode, setContact, onSubmit, error }: { code: string; contact: string; setCode: (value: string) => void; setContact: (value: string) => void; onSubmit: () => void; error: string }) { return <section className="booking-access-card"><div className="booking-access-mark"><LockKeyhole size={20} /></div><span>BOOKING-ID ACCESS</span><h1>Your trip is already<br /><i>waiting for you.</i></h1><p>Use the booking ID shared by your Har Har Mahadev Tours team and the last four digits of the traveler contact to open tickets, invoices, and live updates.</p><label>Booking ID<input value={code} onChange={(event) => setCode(event.target.value)} placeholder="VYG-2026-08456" autoCapitalize="characters" /></label><label>Last four digits of traveler phone<input value={contact} onChange={(event) => setContact(event.target.value.replace(/\D/g, "").slice(0, 4))} placeholder="6264" inputMode="numeric" /></label>{error && <div className="booking-access-error">{error}</div>}<button onClick={onSubmit}>Open my booking <ArrowRight size={17} /></button><small><BadgeCheck size={14} /> No password needed. Access is verified, time-limited, and scoped to this booking.</small></section>; }
