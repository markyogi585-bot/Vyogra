import { motion } from "framer-motion";
import { X } from "lucide-react";

export function SheetFrame({ title, eyebrow, children, onClose, wide = false }: { title?: string; eyebrow?: string; children: React.ReactNode; onClose: () => void; wide?: boolean }) { return <motion.div className="voyagr-sheet-layer" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose}><motion.section className={`voyagr-sheet ${wide ? "voyagr-sheet-wide" : ""}`} initial={{ y: "105%", opacity: 0.7 }} animate={{ y: 0, opacity: 1 }} exit={{ y: "105%", opacity: 0.7 }} transition={{ type: "spring", stiffness: 360, damping: 34 }} drag="y" dragConstraints={{ top: 0, bottom: 0 }} dragElastic={0.13} onDragEnd={(_, info) => { if (info.offset.y > 120 || info.velocity.y > 700) onClose(); }} onClick={(event) => event.stopPropagation()}><div className="voyagr-sheet-grab" aria-hidden="true" />{(title || eyebrow) && <header className="voyagr-sheet-header"><div>{eyebrow && <span className="admin-overline">{eyebrow}</span>}{title && <h2>{title}</h2>}</div><button className="sheet-close" onClick={onClose} aria-label="Close"><X size={19} /></button></header>}{children}</motion.section></motion.div>; }
