import { useState } from "react";
import { ArrowRight, CalendarDays, Minus, Plus, Users } from "lucide-react";
import { SheetFrame } from "./SheetFrame";
import { useTravelSession } from "@/contexts/TravelSessionContext";
export function DateTravelSheet() { const { closeDateSheet } = useTravelSession(); const [guests, setGuests] = useState(2); const [date, setDate] = useState("15 Mar – 19 Mar 2026"); const adjust = (delta: number) => setGuests((current) => Math.max(1, Math.min(10, current + delta))); return <SheetFrame eyebrow="SELECT YATRA DATES" title="When should we hold it?" onClose={closeDateSheet}><div className="date-travel-sheet"><div className="date-choice"><CalendarDays size={19} /><div><span>Departure window</span><b>{date}</b></div><button onClick={() => setDate(date.includes("15") ? "21 Apr – 25 Apr 2026" : "15 Mar – 19 Mar 2026")}>Change</button></div><div className="guest-choice"><Users size={19} /><div><span>Travelers</span><b>{guests} {guests === 1 ? "adult" : "adults"}</b></div><div className="guest-stepper"><button onClick={() => adjust(-1)}><Minus size={15} /></button><b>{guests}</b><button onClick={() => adjust(1)}><Plus size={15} /></button></div></div><div className="date-price-preview"><div><span>Starting total</span><b>₹{new Intl.NumberFormat("en-IN").format(12490 * guests)}</b></div><small>Final total updates after traveler details and add-ons.</small></div><button className="sheet-primary-action" onClick={() => { closeDateSheet(); window.location.assign("/booking"); }}>Continue to traveler details <ArrowRight size={17} /></button></div></SheetFrame>; }
