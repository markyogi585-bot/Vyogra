import { useState } from "react";
import { BellRing, CalendarClock, ImagePlus, Send } from "lucide-react";
export function BroadcastComposer() { const [audience, setAudience] = useState("All travelers"); const [scheduled, setScheduled] = useState(false); const [sent, setSent] = useState(false); return <section className="broadcast-composer"><div className="broadcast-form"><div className="composer-head"><div><span className="admin-overline">PUSH + IN-APP</span><h2>Send a considered signal.</h2></div><BellRing size={22} /></div><label>Audience<select value={audience} onChange={(event) => setAudience(event.target.value)}><option>All travelers</option><option>Wishlist users</option><option>Inactive 30+ days</option><option>Upcoming trips</option><option>One traveler</option></select></label><label>Notification title<input defaultValue="Char Dham Yatra Special Opening" /></label><label>Message<textarea defaultValue="A special yatra departure just opened on a route you saved." /></label><div className="broadcast-media"><button><ImagePlus size={16} /> Add media</button><button onClick={() => setScheduled((current) => !current)} className={scheduled ? "active" : ""}><CalendarClock size={16} /> {scheduled ? "Scheduled" : "Schedule"}</button></div><button className="ops-cta broadcast-send" onClick={() => setSent(true)}>{sent ? "Broadcast queued" : "Preview & queue broadcast"} <Send size={15} /></button></div><aside className="push-preview"><span className="admin-overline">LIVE PREVIEW</span><div className="phone-preview"><div className="phone-top"><span>HAR HAR MAHADEV</span><time>now</time></div><strong>Char Dham Yatra Special Opening</strong><p>A special yatra departure just opened on a route you saved.</p><small>{audience}</small></div><p>Segment-aware delivery, in-app announcement, deep link, draft, and scheduled-send states are ready.</p></aside></section>; }
