import { CheckCircle2, CircleDashed, Cloud, Database, KeyRound, Map, ShieldCheck, WalletCards } from "lucide-react";
const integrations = [{ icon: KeyRound, name: "Firebase Auth", detail: "Google, Apple & Phone OTP", status: "Needs keys" }, { icon: Database, name: "Supabase / RLS", detail: "Traveler & admin data policies", status: "Architecture ready" }, { icon: WalletCards, name: "Razorpay", detail: "Payments, refunds & webhooks", status: "Needs keys" }, { icon: Cloud, name: "Cloudinary", detail: "Image transformation & media", status: "Needs keys" }, { icon: Map, name: "Maps", detail: "Route tracking & destination views", status: "Connected" }, { icon: ShieldCheck, name: "Cloudflare", detail: "WAF, caching & rate limits", status: "Pending edge config" }];
export function IntegrationMatrix() { return <div className="integration-matrix">{integrations.map(({ icon: Icon, name, detail, status }) => <article className="integration-card" key={name}><Icon size={20} /><div><h3>{name}</h3><p>{detail}</p></div><span className={status === "Connected" ? "integration-good" : "integration-pending"}>{status === "Connected" ? <CheckCircle2 size={13} /> : <CircleDashed size={13} />}{status}</span></article>)}</div>; }
